package com.rcbrasileiro.demosso.util;

public class TipoLogadoDetailUtil {


	/*
	 * public static TipoLogadoDetail getTipoLogadoDetail() {
	 * 
	 * TipoLogadoDetail tipoLogadoDetail = null;
	 * 
	 * if (Objects.nonNull(SecurityContextHolder.getContext().getAuthentication()))
	 * {
	 * 
	 * tipoLogadoDetail = (TipoLogadoDetail)
	 * SecurityContextHolder.getContext().getAuthentication().getPrincipal();
	 * 
	 * }
	 * 
	 * return tipoLogadoDetail; }
	 */
}
